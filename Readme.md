In the root folder execute this command to host index.html on port 5050 locally <python -m http.server 5050>
Now to start the server execute <uvicorn server:app --reload> to run it on port 8000 by default.
